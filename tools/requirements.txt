# Core — IPA binary patching (cross-platform, no native deps)
lief>=0.14.0

# Apple ID signing (required for sign / full commands)
cryptography>=41.0.0
requests>=2.28.0
srp>=1.0.20
pbkdf2>=1.3

# Optional — device management via pymobiledevice3
# pymobiledevice3>=2.0.0

# Note: signing also requires omnisette-server running in Docker
# docker run -d --restart always --name omnisette \
#   -p 6969:80 --volume omnisette_data:/opt/omnisette-server/lib \
#   ghcr.io/sidestore/omnisette-server:latest